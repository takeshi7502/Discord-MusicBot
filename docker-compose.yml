services:
  discordmusicbot:
    container_name: discordmusicbot
    build: .
    image: discordmusicbot:local
    network_mode: host
    restart: always
    volumes:
      - ./config.js:/usr/src/app/config.js:ro
