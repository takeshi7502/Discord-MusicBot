services:
  # --------------------------------
  # Lavalink (Audio node) - host network for IPv6 rotation
  # --------------------------------
  lavalink:
    container_name: discordmusicbot-lavalink
    image: ghcr.io/lavalink-devs/lavalink:latest
    network_mode: host
    restart: unless-stopped
    environment:
      - _JAVA_OPTIONS=-Xmx2G
      - SERVER_PORT=2333
      - LAVALINK_SERVER_PASSWORD=youshallnotpass
      - LAVALINK_SERVER_SOURCES_YOUTUBE=false
    volumes:
      - ./Lavalink/application.yml:/opt/Lavalink/application.yml
      - ./Lavalink/plugins/:/opt/Lavalink/plugins/
    healthcheck:
      test: ["CMD-SHELL", "curl -sf -H 'Authorization: youshallnotpass' http://localhost:2333/version || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # --------------------------------
  # Discord-MusicBot - host network to connect to Lavalink
  # --------------------------------
  discordmusicbot:
    container_name: discordmusicbot
    build: .
    image: discordmusicbot:local
    network_mode: host
    restart: always
    volumes:
      - ./config.js:/usr/src/app/config.js:ro
    depends_on:
      lavalink:
        condition: service_healthy
